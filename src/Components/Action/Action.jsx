import React from 'react';

import Dropdown from "../UI/Dropdown"
import './Action.css';

export default function Action({selectedDeployment}) {
    // set up a drop down with the following options:
    return (
        <div className="actions-container">
            <Dropdown
            
            />
        </div>
    )
}